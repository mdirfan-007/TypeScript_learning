var a = 12

function foo() {
    return a + 1
}
console.log(foo())

let arr: [string, number,number] = ["hello", 42,11 ];  

enum userRoles {
    ADMIN = 'ADMIN',
    USER = 'USER',
    GUEST = 'GUEST'
}

userRoles.ADMIN